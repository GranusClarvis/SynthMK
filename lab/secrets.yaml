# SynthMK lab secrets file — referenced by flows as {{ secret.<name> }}.
# LAB VALUES ONLY (they match the demo site's client-side check); for real
# deployments create your own file, chmod 600, and never commit it.
portal_user: monitor
portal_password: synthmk-lab-secret
