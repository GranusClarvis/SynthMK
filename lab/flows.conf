# SynthMK lab schedule.  <flow_file>  <interval_s>  [checkmk_host]
# Short intervals so the demo is snappy.
intranet-home.yaml       30
intranet-overview.yaml   30     intranet-demo
intranet-login.yaml      30     intranet-demo
